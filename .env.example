# Example environment variables for local / deployment
# Vite env vars must be prefixed with VITE_ if used in the client

PORT=5000
NODE_ENV=production

# Example placeholders for optional services
DATABASE_URL=
API_KEY=
